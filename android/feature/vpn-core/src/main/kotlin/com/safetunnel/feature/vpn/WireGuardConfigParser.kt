package com.safetunnel.feature.vpn

class WireGuardConfigParser {
    // For now, we just return the config string as-is; parsing could be added later
    fun parse(config: String): String = config
}
